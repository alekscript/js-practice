// command (alert, prompt, confirm)
alert("Hello"); // alert - modal window with parametr OK

result = prompt(title, [def]); // prompt - modal window with 2 parametrs OK-CANCEL

let isBoss = confirm("Ты здесь главный?");

alert( isBoss ); // выпадает окно с вопросом, если нажат ОК - прилетает true